{
  "name": "growthminds-ai",
  "version": "1.0.0",
  "description": "Root workspace for GrowthMind AI",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix channel-service",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix channel-service\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
